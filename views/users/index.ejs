<h2>Search Users</h2>
<form action="/users" method="GET">
    <label>Name</label>
    <input type="text" name="username" value="<%= searchOptions.username %>">
    <%console.log(searchOptions.username)%>
    <button type="submit">Search</button>
</form>

<br>
<% users.forEach(user => { %>
    <div><%= user.username %></div>
    
<% }) %>